import Box from '@mui/material/Box';
import HeaderBar from '~/components/layout/Header';

const Home = () => {
    return (
        <Box>
            <HeaderBar />
        </Box>
    );
};

export default Home;
